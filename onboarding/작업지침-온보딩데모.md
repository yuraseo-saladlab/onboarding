# 알파앱스 온보딩 데모 — 작업 지침서

> 이 레포를 클론한 뒤 Claude(Cowork)에게 이어서 작업시킬 때, 첫 메시지에 "작업지침-온보딩데모.md 읽고 시작해줘"라고 하세요.
> 작성일: 2026-07-07 · 작성: 서유라 + Claude

---

## 0. 레포 파일 맵

| 파일 | 설명 |
|---|---|
| `onboarding-single-view-v2.html` | ⭐ **현재 작업 중인 파일** — 모든 수정은 여기에 |
| `onboarding-single-view-v1/v3/v4.html` | 과거/실험 버전 — 건드리지 말 것 |
| `demo-review/upsell/push/canvas/instafeed.html` | 앱별 개별 데모 (별도 트랙) |
| `index.html` | 진입 페이지 |
| `CLAUDE.md` | 디자인 대원칙 (Claude가 자동으로 읽음) |
| `작업지침-온보딩데모.md` | 이 문서 |
| `archive/`, `assets/` | 보관용 / 이미지 리소스 |

## 1. 프로젝트 개요

- **작업 파일**: `onboarding-single-view-v2.html` (단일 HTML 파일, 약 6,000줄 — CSS/JS 전부 내장)
- **내용**: 카페24 앱 "알파앱스" 온보딩 데모. 진입 앱은 **알파리뷰**, 추천 앱은 **알파업셀 / 알파푸시 / 알파캔버스**
- **대원칙 (CLAUDE.md)**: "가짜가 아닌 **진짜 실사화 애니메이션**으로 매끄러운 흐름", "실력 있는 UIUX 디자이너가 만든 것처럼 감각적인 디자인"
  - 추상적인 도형 데모 금지 — 실제 서비스 화면(상세페이지, 알림톡, 대시보드)처럼 보여야 함

## 2. 화면 구조

| 구역 | 설명 | 코드 위치 |
|---|---|---|
| **스텝 3-A 히어로** | 알파리뷰 자동 네러티브. 비트 3개(스텝1: 알림톡→리뷰 게시, 스텝2: AI 루틴, 스텝3: 위젯 갤러리). 비트 영역 클릭 = 일시정지/이어보기 토글 | `HERO_PANES` 객체 + `heroGo()/pauseHero()` |
| **스텝 3-B 추천 카드** | 업셀/푸시/캔버스 카드. 카드 상단에 미니 목업(`MICRO` 객체: `.md-cart`, `.md-phone`, `.md-canvas`) | `MICRO` 객체 + `renderPick()` |
| **자세히 보기 오버레이** | 풀사이즈 모달(뷰포트 가득). 앱별 3개 씬(`SCENES.up1~3`, `ps1~3`, `cv1~2` 등), 화면 속 버튼 클릭 시 `.sc.on` 클래스로 스토리 애니메이션 시작 | `SCENES` 객체 + `openDetail()/dtGo()` |

## 3. 확정된 컨셉 (되돌리지 말 것)

- **스텝1**: 자동 발송 아님 — **사장님이 직접 보낸** 리뷰 요청 알림톡. 알림톡 덕에 리뷰 작성이 쉬워져 빠르게 쌓이는 방식. 클릭 한 번 후 리뷰 숫자 **0 → 5,967 카운트업**
- 배지 문구: "리뷰 **바로** 게시 ✓", "댓글 **빠르게 증가** ✓" (자동 게시/자동 완성 아님)
- **스텝3**: "자동 설치" 아님 → **"설치"**
- **푸시 미니 목업**: 위 알림 = 알림톡(파란 알파몰 로고 아바타 "알", 옐로우 배지 없음), 아래 알림 = 문자 SMS(그린 💬 아바타). 프로필은 서로 다르게 유지. 하단 "⚡ 캠페인 자동 발송 중" 필
- **캔버스 미니 목업**: 편집 전(민트, 칩 없음) → 선택(점선 테두리 + "✏️ 편집 중" 칩 등장) → **옐로우** 테마로 교체(주황 금지, 칩 색도 함께 옐로우로) → 편집 종료(칩·점선 사라짐). 4초 무한 루프
- **오버레이 모달**: 작게 쓰지 않음 — 화면 가득(최대 1560px), 스테이지가 남는 높이 전부 차지, 씬 콘텐츠 컬럼(최대 800px)은 가운데 정렬, 씬 잘림 금지

## 4. 코드 패턴 (반드시 따를 것)

- **씬 애니메이션**: 요소 기본 `opacity: 0` + `.sc.on .요소 { animation: ... <delay> both }` 로 시차 연출. 지연 후 사라지는 애니메이션에는 `both` 대신 **`forwards`** (backwards-fill로 시작부터 보이는 버그 방지)
- **숫자 카운트업**: `<b class="cnt" data-to="5967" data-fmt="num" data-delay="2600">0</b>` — `runCounts()`가 처리. 히어로 비트는 `heroGo()`에서 매 사이클 리셋 후 재실행
- **줄바꿈 방지(레이아웃 깨짐)**: 텍스트 컨테이너 `flex:1; min-width:0`, 썸네일·배지·버튼·가격은 `flex-shrink:0` + `white-space:nowrap`
- **그라디언트 배경 전환**: background는 애니메이션 불가 → `::after` 오버레이의 opacity 애니메이션 사용
- **일시정지**: `#heroPanes.paused .hero-pane * { animation-play-state: paused !important }` + 남은 시간 기억(`heroRemain`) 후 이어보기

## 5. 검증 루틴 (수정 후 매번)

```bash
python3 - <<'EOF'
import re
h = open('onboarding-single-view-v2.html').read()
js = '\n'.join(re.findall(r'<script>(.*?)</script>', h, re.S))
open('/tmp/x.js', 'w').write(js)
css = '\n'.join(re.findall(r'<style>(.*?)</style>', h, re.S))
print('CSS braces:', css.count('{'), css.count('}'))  # 반드시 같아야 함
EOF
node --check /tmp/x.js
```

- 새 CSS 클래스를 만들면 마크업(SCENES/MICRO/HERO_PANES)에도 실제로 넣었는지 확인 (한쪽만 고치는 실수 잦았음)
- 오버레이 씬과 3-B 카드 미니 목업(MICRO)은 **서로 다른 컴포넌트** — 같은 내용이라도 따로 고쳐야 함

## 6. Claude에게 시키는 법 (프롬프트 팁)

- 화면 어디인지 명시: "3-A 스텝2에서", "3-B 캔버스 카드 미니 목업에서", "업셀 오버레이 2번 씬에서"
- 스크린샷을 붙여주면 훨씬 정확함 (레이아웃 깨짐은 말로 설명하기 어려움)
- 색·문구는 확정 컨셉(3번 섹션)과 충돌하지 않는지 이 문서를 참조시키기
- 수정 후에는 "검증 루틴 돌리고 파일 공유해줘"라고 요청

## 7. 깃 워크플로우

- 수정 단위마다 커밋 — 검증 루틴(5번) 통과 후에만 커밋할 것
- 커밋 메시지는 화면 기준으로: 예) `3-A 스텝1: 리뷰 카운트업 0→5,967 추가`, `3-B 캔버스 목업: 편집 칩 옐로우 테마 연동`
- v2 파일은 에디터 자동 포맷(멀티라인 CSS)이 걸려 있어 diff가 커질 수 있음 — 포맷 변경과 내용 변경을 한 커밋에 섞지 않기
- 브라우저 캐시 때문에 변경이 안 보이면 강력 새로고침(Cmd+Shift+R)

## 8. 남은 과제 / 아이디어

- 업셀 외 다른 앱(푸시 ps1~3, 캔버스 cv1~2) 씬도 실사 디테일 밀도 높이기 (업셀 수준으로)
- 수치는 전부 예시 — 실데이터 확보 시 교체 (`sc-foot`에 예시 표기 유지)
- 모바일(1단 레이아웃) 실기기 확인
